CREATE TABLE IF NOT EXISTS `TWITTER_USER` (
    ID BIGINT(20),
	NAME VARCHAR(500),
	SCREEN_NAME VARCHAR(500),
	PROFILE_IMAGE_URL TEXT,
    URL TEXT,
    PRIMARY KEY (ID)
);

CREATE TABLE IF NOT EXISTS `USER_PROFILE_IMAGE` (
    ID BIGINT(20),
	IMAGE_URL TEXT,
	IMAGE_LOCATION TEXT,
    PRIMARY KEY (ID)
);

CREATE TABLE IF NOT EXISTS `TWEET_IMAGE` (
    TWEET_ID BIGINT(10),
	IMAGE_URL VARCHAR(500),
	IMAGE_LOCATION TEXT,
    PRIMARY KEY (TWEET_ID, IMAGE_URL)
);

CREATE TABLE IF NOT EXISTS `TWITTER_CHANNEL` (
    ID BIGINT(20),
	NAME VARCHAR(500),
	SCREEN_NAME VARCHAR(500),
	PROFILE_IMAGE_URL TEXT,
    URL TEXT,
    FOLLOWER_COUNT BIGINT(20),
    PRIMARY KEY (ID)
);

CREATE TABLE IF NOT EXISTS `TWEET` (
	ID BIGINT(20),
	TEXT TEXT,
	LINK TEXT,
	LANG VARCHAR(255),
	RETWEET_COUNT INTEGER,
	FAVORITE_COUNT INTEGER,
	RETWEETED BOOLEAN,
	FAVORITED BOOLEAN,
	TWITTER_USER_ID BIGINT(20),
	TWITTER_USER_NAME TEXT,
	CREATED_AT DATETIME,
	ORIGINAL_TWITTER_NAME TEXT,
	ORIGINAL_LINK TEXT,
	ORIGINAL_CREATED_AT DATETIME,
	ORIGINAL_TEXT TEXT,
	PRIMARY KEY (ID)
);